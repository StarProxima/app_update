name: app_update_apk_installer
description: "A new Flutter plugin project."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.7.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  app_update:
    path: ../.. # TODO поменять на путь с Pub-а

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  project_kit:
    git:
      url: https://github.com/StarProxima/dev_kit
      ref: develop
      path: unpublished/project_kit

flutter:
  plugin:
    platforms:
      android:
        package: com.blancvpn.app_update_apk_installer
        pluginClass: AppUpdateApkInstallerPlugin