name: in_app_update_installer
description: "A Flutter plugin for in-app update installation on Android."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.7.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  app_update:
    path: ../.. # TODO поменять на путь с Pub-а
  in_app_update: ^4.2.5
  path_provider: ^2.1.5
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  pub_semver: ^2.2.0

  project_kit:
    git:
      url: https://github.com/StarProxima/dev_kit
      ref: develop
      path: unpublished/project_kit
